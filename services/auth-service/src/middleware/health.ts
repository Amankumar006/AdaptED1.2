/**
 * Health and metrics endpoints for Kubernetes probes
 */

// Deprecated: health endpoints are implemented in app.ts using monitoringService.
// Keeping file minimal to avoid duplication.
export {};
